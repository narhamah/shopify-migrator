[pytest]
testpaths = tests
pythonpath = src
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    acceptance: live post-build acceptance checks against a destination store (skipped unless configured)
